# Copy to .env if you want to record NEW model replies with `python run.py warm`.
#
# The demo does not need this. Every verdict on the demo screen replays from
# agent/cache/, which is committed on purpose so the whole thing runs with the
# network unplugged. Without a key the agent falls back to templates and every
# verdict on screen says so.
GEMINI_API_KEY=
