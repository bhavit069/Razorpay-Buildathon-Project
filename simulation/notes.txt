NOTES
=====
What exists and what it does. Newest at the bottom.


2026-08-25
----------
generator - invents a year of fake payment traffic, 100k orders across 8 shops,
  plus a fraud system that blocks some of them. it never decides in advance
  which blocks are mistakes. it builds a world first, then runs a blocker that
  can't see the truth, and the mistakes fall out.

signal checker - asks whether the blocked pile is separable at all, and whether
  knowing a customer's history at other shops helps.


2026-08-26 morning
------------------
development folder - the working repo. simulation/ is now an archive.

task runner - python run.py <thing>. make isn't installed here. also forces
  UTF-8 because the scripts print rupee symbols and windows consoles choke.

bug: the generator only ever made 12,000 customers regardless of how many
  orders you asked for. 300k orders meant 25 orders per customer instead of 8,
  which made everyone look like a regular and pushed the block rate from 2.8%
  to 6.3%. added a flag so the customer count scales with the order count.

the two design docs quote numbers from an older generator. row counts, block
  rates, the moat table. audit is in README.


2026-08-26 afternoon - decision system
--------------------------------------
truth vault - the answer key behind one module with two entry points. training
  can only ask about the train split and errors otherwise. scoring sees
  everything. cheating has to be deliberate.

feature store - hands you one blocked order's evidence in three labelled piles:
  what the shop could see, what only the network can see, and bookkeeping.
  switching between "shop only" and "shop plus network" is one argument, so the
  moat comparison can't become two different programs.

model - estimates the chance a block was correct, then a calibration step maps
  those onto a scale where 0.2 means one-in-five. the next layer spends them
  like prices so the scale has to be right.

policy - turns the estimate into one of four actions using arithmetic about
  money rather than a fixed cutoff. release, uphold, ask the customer, hand to a
  person. the same estimate gives different actions on a Rs 500 order and a
  Rs 5,00,000 one.

the policy can't release anything it's more than 20% unsure about, and no
  setting changes that. it falls out of the arithmetic: you earn a quarter of a
  sale but lose the whole basket, so past a certain doubt the sum is negative
  regardless of order size. margin sets the ceiling, not risk appetite. the
  sweep table in the pitch doc runs to 0.5, which an EV rule can't reach.

replay - walks the blocked pile in date order and records every decision. truth
  is joined afterwards, never during. two runs produce identical files.

scorer - joins truth at the end and prices everything. reports recovered and
  fraud admitted side by side, with error bars.

report writer - regenerates METRICS.md. every number the pitch quotes comes out
  of here.

releasing everything makes more gross revenue and still loses money once margin
  is counted properly. strongest single argument for having a policy.

the moat is real but small. measured it at +63% earlier by comparing both models
  at the same cutoff, which was wrong because the shop-only model's raw scores
  sit on a different scale. once both are calibrated and each gets its own
  operating point it's about +5%. the claim that holds isn't the size, it's that
  network evidence releases more orders at higher precision at the same time.
  moving a cutoff can't do both.

three test files - can training see the answer key (no), does signal vanish when
  labels are scrambled (yes, 0.91 to 0.49), can a feature know the future (no,
  3401 checks), does the policy behave, are two replays identical. 33 tests.

bug: a case the model was certain was fraud got sent to "ask the customer"
  instead of "uphold". the thin-file guard was running before the confidence
  check. reordered. keeping a block needs no evidence because the block is
  already there, so the guard only has to protect the release direction.

notebooks - five, generated by a script so their printed output can't drift from
  the code. 00 map, 01 data, 02 the moat three ways, 03 probability to decision,
  04 the six pitch cases.

five of the six showcase cases are in the training half. demoing them means
  demoing on cases the model studied. notebook 04 finds holdout replacements.

the Kolkata case doesn't behave as the pitch says. an 88-order customer with
  three years and a spotless record scores 0.22, so the system abstains instead
  of releasing. filed as a model miss at the time. it isn't one - see the
  2026-08-27 entry.


2026-08-27 - accuracy pass before pushing
-----------------------------------------
seed check - reran the whole pipeline on five independently generated worlds.
  net contribution lands anywhere between Rs 1.15 cr and Rs 1.87 cr. that spread
  is wider than the confidence interval we report inside a single world, which
  means the rupee numbers are limited by how the world was generated, not by how
  many cases the holdout has. precision is stable at 97.8-99.1%. AUC lift ranges
  +0.043 to +0.072. seed 42, which everything else uses, is the worst of the
  five.
  -> quote precision as a number, quote money as a range.

bug: the confidence intervals resampled cases independently, but 36% of holdout
  cases belong to a customer who appears more than once, and those share a
  network file and a device. now resamples customers. turned out to change the
  intervals by less than 10%, but the old way was wrong in principle.

METRICS.md claimed the holdout was scored once. not true. it's read for the
  headline, four baselines, an 11-point frontier, per-merchant, three ablations,
  two sensitivity grids and the failure exhibit. what's actually true is that
  nothing was CHOSEN on it. reworded.

77% of holdout value is one merchant (Aurum Jewels, 451 of 1775 cases). the
  rupee headline is mostly a measurement of that one shop's blocked pile.

LightGBM's default feature importance is split count, which is flat and ranked
  network_clean_rate sixth at 0.085. that made the data card's warning about
  that feature look resolved. gain puts it first at 0.346, so the warning is
  confirmed. now reports both.

stripped the writing back across the docs and code comments. it had drifted into
  pitch prose.


OPEN at this point
------------------
- retune the generator so shop-only evidence is weaker (pitch assumes 0.81, it's
  0.87), or keep the data and rewrite the moat claim around shape. leaning
  toward the second.
- the Kolkata-type miss: long spotless files scoring too high.
- DATA_CARD.md and IDEA.md still carry stale numbers.
- determinism verified on one machine only. float behaviour across platforms
  untested.


2026-08-27 later - phase 2, the agent
-------------------------------------
ledger - append-only log where each entry hashes the previous one. rewriting a
  past decision breaks every hash after it, so the file shows tampering. tested
  by editing an entry and checking the chain complains about that exact one.

llm client - one entry point to Claude with a disk cache keyed on the inputs.
  three modes: live calls the API and records the reply, replay serves only from
  cache and errors on a miss, offline falls back to a deterministic template.
  offline output is tagged source="template" and never claims to be model
  output. no API key on this machine so everything currently runs offline.

tools - the only path from the agent to anything that moves money. thin wrappers
  over phase 1, every call logged. none of them takes text as an argument, so
  there's no signature through which generated prose could reach the policy.

verdict writer - model gets the evidence and the decision that was already made,
  writes 3-6 sentences. then every number in the prose is checked against the
  evidence and regenerated if it invented one. 300 of 300 clean on the last run.

  found a bug in that check: amounts print rounded to whole rupees, so a Rs
  641.76 order shows as "642", and the checker rejected it as invented. now
  allows the rounded form of any value.

stepup - two model roles, agent asks and a simulated customer answers, seeded
  from ground truth so honest customers answer and fraudsters evade. a third
  call extracts structured facts. only the facts come back; the model's opinion
  of the conversation is thrown away.

orchestrator - the case loop. adjudicate, decide, maybe verify, write, log.

got the step-up mechanism wrong twice before getting it right:

  first version zeroed the customer's past return count when they agreed to
  prepay. that's falsifying history - prepaying removes FUTURE return risk, it
  doesn't mean the returns never happened.

  second version credited verification as extra prior orders. same mistake in a
  different costume. and it backfired: adding 3 orders to a thin file in the
  same tenure looks like velocity, so two customers who verified successfully
  came out scoring WORSE than before they answered (0.204 -> 0.571).

  the actual fix: verification isn't history and doesn't belong in the features
  at all. it goes to the insufficiency gate, which is asking "can we identify
  this person", and leaves p_bad completely alone. the record didn't change so
  the estimate of the record shouldn't either. tested.

then: only 2 of 75 step-ups resolved anything, because most step-ups come from
  the ambiguous branch where the gate was never the blocker. running a dialogue
  there can't change the outcome. now only insufficiency step-ups get an
  exchange - 4 instead of 75, and both that resolved were correct.

on 300 holdout cases: 151 released at 99.3% correct, 75 upheld, 73 held for
  verification, 1 escalated. all 300 verdicts audit-clean, ledger verifies.

worth noting: upholds are only 56% correct. at cap=0.02 the system is refusing
  a lot of good customers to protect precision on the releases. that's the
  conservative end of the frontier doing what it says.


STILL OPEN
----------
- generator retune vs reframing the moat claim (see above, unresolved)
- the Kolkata-type miss: long spotless files scoring too high
- DATA_CARD.md and IDEA.md still carry stale numbers
- determinism verified on one machine only
- no API key on this machine, so no verdict has been through Claude yet. the
  code path is written and cached-replay is ready; needs one live run to record
  the cache.
- service/ and the UI are not started.
- the ambiguous-branch step-ups (73 of 300) have no path to resolution. either
  give verification an EV effect for them or accept they stay abstentions.


2026-08-27 evening - hardening
------------------------------
tested the claim the whole two-phase design rests on: that wrapping the decision
  system in an agent doesn't change what it decides. ran both over the same 300
  cases. p_bad identical on all 300 (difference exactly 0.0), and the only two
  action differences are the two step-ups that resolved, which the backtest had
  no way to run. now a permanent test, and demo.py rechecks it every run.

the agent's decisions are now priced by the same grader the backtest uses,
  instead of a separate accounting. as_core_ledger() re-expresses an agent run
  as a phase 1 ledger so core.metrics.grade works on it unchanged. on 300 cases
  at cap=0.02: 151 released, 99.3% precision, Rs 73.06L recovered against
  Rs 1,494 admitted.

escalation briefs. the spec calls escalation a first-class outcome with a
  written brief, but it was just reusing the verdict. now has its own prompt and
  template addressed to a reviewer rather than a merchant: what's being asked,
  what the record shows, what's missing, what would settle it. citation-checked
  like a verdict.

latency percentiles instead of a bare median. p50 0.78ms, p95 1.03ms, max
  4.54ms. that's offline templates; a live run will be dominated by API time.

61 tests.


2026-08-27 - review response
----------------------------
ten items from review. all of them make a headline number worse. took all of
them.

renaming was already done, 0 ACQUIT left anywhere.

1. deployment mode is now a declared assumption sitting next to the step-up pass
   rates. booking the full order value as recovered is only honest if we run
   inline at checkout with the customer still in session. a queue review means
   they already left, so only some come back. Deployment(inline|queue) with a
   recontact rate, swept in METRICS.md 9. at 35% return the same decisions are
   worth Rs 0.12cr instead of Rs 1.29cr. the discount applies only to recovery,
   not to fraud admitted: a fraudster invited back to finish a stolen-card order
   is more motivated to return than an honest customer who already bought
   elsewhere.
   inline also means step-up has to complete in session. an email round trip is
   a queue review wearing an inline costume.

2. stopped demoing at cap=0.02. that number was picked because it made precision
   look good, which is the exact behaviour the whole thesis criticises. now runs
   at the EV point, cap=0.20, which is where EV(release) stops being positive.
   precision fell 98.6% -> 96.3%, fraud admitted Rs 22.57L -> Rs 51.21L, recall
   rose to 88.2%. worse-looking and correct.

3. recall of recoverable now leads the headline table in bold, with the
   abstention rate and the raw count of undecided cases next to it. precision is
   the constraint, not the goal. in a product about getting wrongly blocked
   customers through, the share of them that got through is the number.

4. leave-one-merchant-out. dropped 1,946 Aurum training cases, tested on the 451
   Aurum holdout cases. AUC falls 0.0008, which is nothing - the model ranks an
   unseen merchant about as well as one it trained on. but precision at the same
   cap drops 92.9% -> 89.0% and contribution by Rs 31L, because the probabilities
   are calibrated to merchants it has seen. so the platform claim holds for the
   hard part, learning who is safe from cross-merchant behaviour, and fails for
   the easy part, which is recalibrating on a few hundred of the new merchant's
   own cases before trusting the operating point.

5. human reviewer baseline added: 87.5% accuracy, Rs 150 a case, ~4 hours.
   they beat us. Rs 1.36cr against our Rs 1.29cr on judgment alone. the row that
   doesn't survive is the assumption behind it - that a person reviews all 1,775
   cases. review is rationed to the top few percent by value because of what it
   costs. the honest claim is not that this is smarter than a reviewer, it's
   that every case gets reviewed instead of the top 2%.

6. generator retune: closed by taking the reframe. the moat claim is that
   network evidence releases more orders at higher precision at the same time,
   and moving a cutoff can't do both. one sentence, two numbers. the AUC
   ablation is a diagnostic and belongs in the appendix, not the pitch.

7. Kolkata miss: closed, not a bug. an 88-order spotless file scoring 0.22 is
   the bust-out prior doing its job. the dataset has 42% of fraudsters farming
   clean history on purpose, so a long clean record can't be a free pass. the
   model declining to treat it as one is the feature.

8. live model run: done, partially. gemini key arrived, so llm.py now has two
   providers behind the same complete() entry point and picks whichever
   credential is present. the anthropic path is untouched.

   four things went wrong, all worth writing down:
   - TLS failed on this machine even outside the sandbox. something intercepts
     it with a cert that's in the windows store but not certifi's bundle.
     truststore.inject_into_ssl() fixes it.
   - the .env has "GEMINI_API KEY" with a space, which no shell will load.
     read_env() tolerates it. worth renaming to GEMINI_API_KEY.
   - gemini 3.x thinks by default and max_output_tokens caps thinking plus
     visible text together. a 600-token budget spent 432 on thinking and
     returned 54 of prose, cut mid-sentence. verdicts now get 3000 tokens of
     headroom on top of their own budget.
   - free tier is 20 requests per day PER MODEL. gemini-flash-latest is an
     alias for 3.7-flash, whose bucket we'd already spent. switched to
     3.6-flash, which has its own.

   and one real bug in our own code, which only showed up because a live model
   was finally exercising it: the citation checker pulled digits out of payment
   ids. "pay_5E72cODtQrmZkn" contains 72, which is not in the evidence, so
   every correctly-cited verdict failed the check three times and fell back to
   the template. the offline runs never caught it because the template doesn't
   print the payment id. so "300 of 300 audit clean" was measuring a checker
   that would have rejected real output. fixed, and it still catches
   fabrication.

   3 demo cases now carry real gemini verdicts, citation-checked and cached.
   the rest hit the daily cap. warm_cache.py records the demo cases only, which
   is what a demo needs; replay serves them off-network.

9. showcase cases are picked by predicate now, in core/showcase.py. six roles
   described by shape, matched against holdout, ranked by each role's own
   tiebreak. survives a reseed, and no case from the train split can sneak in.
   metrics harness emits artifacts/tables.md so the other docs stop being
   hand-patched.

10. UI cut to one screen. service/case_room.py writes a single self-contained
    html file: case list, local vs network evidence side by side, verdict, tool
    trace, transcript. no server, no build step, opens from disk. the frontier
    and portfolio views stay in METRICS.md and the notebooks, which carry them
    better anyway.

11. checked the queue haircut because it looked like a bug. 35% recontact
    turning 1.29cr into 11.5L reads like the discount is being taken twice.
    it isn't. it's applied once, to recovery, and net contribution is computed
    off the already-discounted number. wrote the whole thing out term by term
    in METRICS.md 11 and added tests that assert the published rows reproduce
    grade() to the paisa.

    the reason it drops that hard: only one term scales.
      net = 0.25 * R_gross * rate - A - 750*n_bad - reviews
    margin is 25% of a recovered rupee, fraud is 100% of an admitted one, so a
    fixed 51.58L of drag comes off a quarter of a shrinking number. cushion at
    cap 0.20 is only 3.5x. breakeven is a 28.6% return rate, which is a better
    thing to quote than any single point in the band.

    also stopped reporting one rate. 70/50/35 now, because an in-session retry
    prompt is not a next-day email. and the asymmetry assumption (fraudsters
    come back more readily than honest customers) is declared at the top of
    the section instead of sitting in a dataclass default.

12. added a reviewer baseline that reflects what merchants actually do. the
    old row assumed a person works all 1775 cases. nobody does. rank by order
    value, work down until the day runs out, everything below the line stays
    blocked. same reviewer, same 87.5%, charged only for what they reach.

      all 1775      88.3% recall   1.36cr    (beats us)
      top 10%        8.6% recall   69.41L
      top  3%        2.4% recall   28.17L
      us, all 1775  88.2% recall   1.29cr

    the full-review row stays in. losing to it is the honest result. the two
    rows together are the actual claim: a reviewer wins case for case, nobody
    can afford one across the whole pile, so most cases are never looked at.

13. froze METRICS.md, and freezing it turned up a bad number. the seed sweep
    was re-picking the cap on each seed's own calibration slice, so the range
    it produced described a different policy than the headline did. worse,
    re-tuning per seed absorbs exactly the variation you're trying to measure.
    every seed is graded twice now and the headline quotes the shipped row.

                      tuned cap (old)        shipped cap 0.20 (now)
      contribution    1.15 - 1.87 cr         0.90 - 1.86 cr
      precision       97.8% - 99.1%          92.0% - 98.2%
      recall          not reported           83.1% - 89.6%
      fraud admitted  not reported           8.13L - 51.21L

    so "precision is the stable number" is withdrawn. it moves 6.2 points
    across five worlds at a fixed operating point. recall is the steady one.
    seed 42, which we quote everywhere, is middling on revenue and the worst
    of the five on cost.

14. housekeeping. renamed GEMINI_API KEY to GEMINI_API_KEY, the space meant no
    shell would ever have loaded it. added run.py room and run.py warm so the
    case room and the cache warmer are reachable like everything else. pulled
    the em dashes out of DATA_CARD.md and .gitignore.

    and a git thing worth writing down: the outer folder and pr/ are two
    different repositories. pr/ is the real clone of the github repo and is on
    dev tracking origin/dev. the outer folder is local-only and its history has
    no common ancestor with github at all. push from pr/. i had this wrong
    earlier and said there was no remote.

15. found a bug while checking the demo actually demos anything. the case room
    ran the orchestrator over the first 400 holdout cases in chronological
    order and then tagged whichever of them matched a showcase role. five of
    the six showcase cases sit past position 400. so the page rendered exactly
    one tagged case, and none of the three recorded gemini verdicts were ever
    requested, which meant every verdict on the demo screen was a template.

    the header even said "1 tagged as demo cases" and nobody read it, me
    included. the showcase cases are now put in explicitly and the stream
    fills the rest of the limit. three of the six now carry real model output,
    including the spotless-record-still-refused case, which is the one worth
    opening first.

    worth noting what the shape of this bug was: nothing threw, nothing failed
    a test, the page looked fine. it was only visible by counting verdict
    sources on the generated html.

    small thing left alone: the model writes booleans as counts, "0 address
    mismatches", "0 cash on delivery". reads a bit odd. it's a prompt wording
    issue, not a correctness one, and metrics are frozen.

16. warmed all six showcase cases. the quota is per day per model and it had
    rolled over, so the three that were missing went through on the first try.
    every demo case now carries real model output instead of half of them.

17. provenance is on the screen now, which was the point. the case room used
    to print "verdict source: cache" in small grey text, which is technically
    honest and useless. now: a green badge naming the model, or an amber one
    reading TEMPLATE, NOT MODEL OUTPUT, plus a line under each verdict saying
    which it is in words, plus MODEL/TMPL tags in the list, plus a header
    stating the split. a screenshot of the page cannot imply model output that
    isn't there.

    threading that through turned up two dead strings. Completion never
    produced a source called "claude", so the orchestrator summary bucket for
    it always read 0, and stepup.py tested `sources & {"claude","cache"}` and
    reported "template" for exchanges a model had actually written. both fixed.
    the test that was supposed to catch this asserted every verdict was a
    template, which was only true while the cache was empty.

18. IDEA.md and DATA_CARD.md regenerate now. core/docs.py fills marked blocks
    in place, so the prose stays hand-written and the numbers stop being.
    seven blocks: portfolio context, the six worked examples, the frontier,
    the results table, the calibration table, the file table, the outcome mix.
    `python -m core.docs --check` fails if a document has drifted.

    IDEA.md section 5 was the worst of it. five hand-picked payment ids from
    the 100k dataset, five of six in the train split, quoting rupee figures
    from a generator that has changed twice since. it now prints whatever
    core/showcase.py selects out of holdout.

19. dry_run.py. replaces socket.socket with something that raises, then does
    everything the demo does: doc blocks current, suite green, model fits, six
    cases picked from holdout, agent over 300 orders, agent matches the
    backtest to the digit, case room builds with every demo case on it and
    model-backed, page loads nothing external, replay serves the demo cases.
    12 checks. ran it twice, clean both times.

    the point isn't that it passes. if it needs a code change to pass, that's
    the finding, and better now than in front of a room.

20. wrote SCRIPT.txt at the root: five beats, what to say, which case to open
    for each, and a list of numbers that are allowed to be said out loud along
    with three that aren't. the four-things-that-got-worse part is beat five,
    not a footnote.

    also flow.html, a browser walkthrough of the pipeline. one real case,
    pay_5E72cODtQrmZkn, carried through all eight stages with a status bar
    that fills in as you walk it. self-contained, opens from disk.
