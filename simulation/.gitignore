# generated data, regenerate with `python run.py data300k` (deterministic given seed)
data/
data300k/
artifacts/*
# generated, but these two are committed on purpose. tables.md is the block the
# other documents quote, so it must not drift from what the harness produced.
# case_room.html is the demo: a judge who clones this can open it without
# running anything first.
!artifacts/tables.md
!artifacts/case_room.html
!artifacts/dashboard.html
*.pkl

# generated report
METRICS.md

__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ipynb_checkpoints/
.env
